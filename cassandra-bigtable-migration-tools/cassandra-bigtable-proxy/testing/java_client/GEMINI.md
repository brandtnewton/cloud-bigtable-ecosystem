# Overview

This is an integration testing module for the Bigtable to Cassandra Proxy which translates CQL into Bigtable operations.
The purpose of this module is to ensure that the Datastax Java client is compatible with the Proxy.

These tests assume the Proxy is already running locally.
